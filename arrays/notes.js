/* const notes = ['Note 1', 'Note 2', 'Note 3']

console.log(notes.length)

notes[0] = 'Note 12'
console.log(notes[0])
console.log(notes[notes.length - 3]) */

const notes = ['Note 1', 'Note 2', 'Note 3']

/* console.log(notes.pop())  //remove the last element
notes.push('Note 4')

console.log(notes.shift())
notes.unshift('My first Note') */

//notes.splice(1,1)
//notes.splice(1,0,'This is the new 1st index but second item')
//notes.splice(1, 1, 'This is the new 1st index but second item')



console.log(notes.length)
console.log(notes)


console.log(notes.indexOf('Note 2'))

